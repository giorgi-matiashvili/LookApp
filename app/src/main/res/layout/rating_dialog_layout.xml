<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/rating_bar_current_rating"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0.0"
        android:textSize="28sp"
        android:gravity="center_horizontal"
        android:layout_margin="10dp"
        />

    <RatingBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="10"
        android:stepSize="0.2"
        android:rating="0"
        style="@style/foodRatingBar"
        android:id="@+id/spot_details_rating_bar"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="15dp"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/rating_dialog_btn_rate"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="@string/spot_rating_dialog_rate_btn_text"
            android:layout_marginRight="5dp"
            android:background="@drawable/round_button_selector"
            android:textColor="@android:color/white"
            android:layout_height="wrap_content" />

        <Button
            android:id="@+id/rating_dialog_btn_cancel"
            android:layout_marginLeft="5dp"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="@string/spot_rating_dialog_cancel_btn_text"
            android:background="@drawable/round_button_selector"
            android:textColor="@android:color/white"
            android:layout_height="wrap_content" />

        </LinearLayout>

</LinearLayout>